# Check if the file exists
ls alx_travel_app/requirement*.txt

# Check if it is empty
if [ -s alx_travel_app/requirement.txt ]; then
  echo "✅ File exists and is not empty."
else
  echo "⚠️ File missing or empty."
fi
